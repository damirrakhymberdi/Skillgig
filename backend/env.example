## Backend (FastAPI)
#
# Copy this file to `backend/.env` (not committed) for local dev / production env config.
APP_ENVIRONMENT=development

# SECURITY: change in production
APP_SECRET_KEY=change-me-super-secret-key

# SQLite local dev default:
APP_DATABASE_URL=sqlite:///./data/app.db

# Comma-separated allowed origins for the SPA (production should NOT use "*")
APP_CORS_ORIGINS=http://localhost:5174,http://127.0.0.1:5174


